<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Confirmation d'abonnement</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 800px; margin: 0 auto; padding: 20px; }
        .header { text-align: center; margin-bottom: 30px; }
        .header h1 { color: #f36100; }
        .details { border: 1px solid #ddd; padding: 20px; border-radius: 8px; }
        .details p { margin: 10px 0; }
        .footer { text-align: center; margin-top: 30px; font-size: 12px; color: #777; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Confirmation d'abonnement</h1>
            <p>Émis le {{ export_date|date('d/m/Y') }}</p>
        </div>
        <div class="details">
            <p><strong>Utilisateur :</strong> {{ user.prenom }} {{ user.nom }}</p>
            <p><strong>Type d'abonnement :</strong> {{ abonnement.type.value|capitalize }}</p>
            <p><strong>Activité :</strong> {{ abonnement.activite.nom }}</p>
            <p><strong>Date de début :</strong> {{ paiement.dateDebut|date('d/m/Y') }}</p>
            <p><strong>Date de fin :</strong> {{ paiement.dateFin|date('d/m/Y') }}</p>
            <p><strong>Montant :</strong> {{ paiement.amount }} DT</p>
            <p><strong>Statut du paiement :</strong> {{ paiement.status|capitalize }}</p>
        </div>
        <div class="footer">
            <p>Merci pour votre abonnement ! Contactez-nous en cas de questions.</p>
        </div>
    </div>
</body>
</html>