<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Boutique{% endblock %}</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
        <style>
            .nav-link.position-relative .badge {
                transform: translate(-50%, -50%) !important;
                margin-left: -0.5rem;
            }
        </style>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container">
                <a class="navbar-brand" href="{{ path('shop_product_index') }}">Boutique</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('shop_product_index') }}">Produits</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('shop_orders_index') }}">
                                <i class="bi bi-list-check"></i> Mes Commandes
                            </a>
                        </li>
                    </ul>
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link position-relative" href="{{ path('shop_cart_index') }}">
                                <i class="bi bi-cart"></i> Panier
                                {% if cart_count() > 0 %}
                                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                                        {{ cart_count() }}
                                        <span class="visually-hidden">produits dans le panier</span>
                                    </span>
                                {% endif %}
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

        <div class="container mt-4">
            {% for message in app.flashes('success') %}
                <div class="alert alert-success">
                    {{ message }}
                </div>
            {% endfor %}

            {% block body %}{% endblock %}
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
        {% block javascripts %}{% endblock %}

        <footer class="bg-light py-4 mt-5 border-top">
            <div class="container">
                <div class="row text-center align-items-center justify-content-center mb-2">
                    <div class="col-12">
                        <h5 class="fw-bold" style="letter-spacing:1px;"><span style="font-style:italic;">BOUTIQUE DE PRODUITS MUSCULATION ET FITNESS</span> <span class="text-primary">@GYMIFY</span></h5>
                    </div>
                </div>
                <div class="row text-center justify-content-center g-3">
                    <div class="col-6 col-md-2">
                        <i class="bi bi-geo-alt fs-2"></i>
                        <div class="fw-bold">Entreprise familiale</div>
                        <div class="text-muted small">100% française</div>
                    </div>
                    <div class="col-6 col-md-2">
                        <i class="bi bi-lightning-charge fs-2"></i>
                        <div class="fw-bold">Expert n°1 depuis 2006</div>
                        <div class="text-muted small">Élu meilleur site</div>
                    </div>
                    <div class="col-6 col-md-2">
                        <i class="bi bi-truck fs-2"></i>
                        <div class="fw-bold">Livraison moins de 24h</div>
                        <div class="text-muted small">Expédié le jour même</div>
                    </div>
                    <div class="col-6 col-md-2">
                        <i class="bi bi-box-seam fs-2"></i>
                        <div class="fw-bold">+ 3 Millions de colis</div>
                        <div class="text-muted small">Préparés par nos soins</div>
                    </div>
                    <div class="col-6 col-md-2">
                        <i class="bi bi-hand-thumbs-up fs-2"></i>
                        <div class="fw-bold">Avis clients</div>
                        <div class="text-muted small">
                            <span class="text-warning">&#9733;&#9733;&#9733;&#9733;&#9733;</span> 4,7/5
                        </div>
                    </div>
                </div>
            </div>
        </footer>
    </body>
</html> 